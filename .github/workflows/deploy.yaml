name: Generate README and Deploy to Main

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout dev branch
        uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0

      - name: Set up Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run README generator
        run: bun run build

      - name: Commit to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Sauvegarder le README généré dans un fichier temporaire
          cp README.md /tmp/generated-readme.md

          # Changer de branche
          git checkout main

          # Écraser le README de main avec celui généré
          cp /tmp/generated-readme.md README.md

          # Commit & push
          git add README.md
          git commit -m "chore: update README [auto]" || echo "No changes to commit"
          git push origin main
          git add README.md
          git commit -m "chore: update README [auto]"
          git push origin main
